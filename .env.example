# 複製成 .env 後填入實際值
DATABASE_URL="file:./dev.db"
SESSION_SECRET="請改成至少 32 字元的隨機字串"

# 選填：正式站若不是用 HTTPS（例如公司內網用純 HTTP 部署），
# 要設成 false，否則登入 session cookie 會被瀏覽器丟棄、一直被導回登入頁。
# 有 HTTPS（如 Vercel）不用設這個變數，預設就是安全的。
# COOKIE_SECURE=false
